# ai-learning entry format (mx-flow Phase 6.3)

> Canonical format for `~/.mx/<project>/ai-learning.md`, the project-level
> learning log. Phase 6.3 points here instead of restating the schema.
> Edit this file to change the format — `install.sh` preserves local
> changes to `references/*`.

| Date       | Issue or Learning | Root Cause | Prevention Rule |
| ---------- | ----------------- | ---------- | --------------- |
| YYYY-MM-DD | <what happened>   | <why>      | <how to avoid>  |

Acceptable entries: techniques confirmed, observations, rules verified.
